# Graupner Suite — Vorgehen / Maßnahmenplan (Stand 17.06.2026)
**Grundlage:** alle eigenen Befunde aus Workflow-Vergleich + Code-Scan. **Read-only erstellt, kein Code geändert.**

## Leitprinzipien (für jeden Schritt)
- Kopf über jedem Auftrag: „⚠️ CREDIT-LIMIT: MAX 1 CREDIT. BEI ÜBERSCHREITUNG SOFORT STOPPEN."
- **Eine Aktion → stoppen → fragen „Soll ich weitermachen?"**. Ralph entscheidet.
- **Vor Aufräum-/Lösch-/Migrationsschritten: DB-Snapshot** (Regel 5) + Einzel-Freigabe.
- Gesperrt (nicht anfassen): `pdf_generator.py`, `routes_v1.py`, `dokumente_v2`, `dokumente_v6`, Live-DB.
- Änderungen liegen auf **Preview** → Live nur per „Save to Github" + Redeploy durch Ralph.

---

## PHASE 0 — Stabilität & aktuelle Fehler (zuerst, klein)
Ziel: Was im Alltag stört, sauber abschließen.
| Maßnahme | Aufwand | Risiko |
|---|---|---|
| 0.1 Heute geänderte Dinge auf Live bringen (GitHub-Save + Redeploy) – durch Ralph | – | gering |
| 0.2 Wartungs-Tab auf Live sichtbar (folgt aus 0.1) | – | gering |
| 0.3 Restliche „aktuelle Fehler" sammeln, die dir beim Arbeiten auffallen | je 1 Credit | gering |

## PHASE 1 — Kernlücken Versand & Rechnungswesen (höchster Nutzen)
| # | Maßnahme | Warum | Aufwand | Voraussetzung |
|---|---|---|---|---|
| 1.1 | **Option B: Direkter Mailversand aus der Suite (Jimdo-SMTP)** – `/email/document` per „Direkt senden"-Button verdrahten | Täglicher Mailverkehr ohne Umweg über Mailprogramm; Profi-HTML+PDF; protokolliert | 1–2 Credits | **Jimdo-SMTP-Daten** + Integrations-Experte |
| 1.2 | **Zahlungseingang-Tracking** (Schritt 11) – Status „bezahlt/offen", Zahldatum, offene Posten | Größte echte Lücke im Rechnungswesen | 2–3 Credits (eigene Phase) | Klärung: manuell oder Bank-Import? |

## PHASE 2 — Dashboard als „Cockpit" neu aufbauen
(Dashboard ist aktuell ausgeblendet, Aufgabe `95b4b4e8…` vorgemerkt.)
| Maßnahme | Aufwand |
|---|---|
| 2.1 Kacheln: Anfragen (ungelesen), Angebote (offen/Beauftragt), **Auftragsbestätigungen**, **Rechnungen** (offen/bezahlt), Mahnwesen, Termine heute | 1–2 Credits |
| 2.2 Zähler-Definitionen korrigieren (z. B. „Offene Angebote" = Entwurf + Versendet) | inkl. 2.1 |
| 2.3 Dashboard wieder einblenden + Startseite zurückstellen (oder Termine belassen) | 1 Credit |

## PHASE 3 — Automatisierung (mittel- bis langfristig)
| # | Maßnahme | Lücke aus Vergleich |
|---|---|---|
| 3.1 | **Automatischer Mail-Eingang** (Anfragen landen automatisch in der Suite) | 🟥 |
| 3.2 | **Mail → Projekt/Angebot** halbautomatisch verknüpfen | 🟥 |
| 3.3 | **KI-Angebots-Assistent** (Anfrage → Angebotsentwurf) | 🟥 |
| 3.4 | **Kalender-Live-Sync** (z. B. Google) – heute nur iCal-Export | 🟥 |
*(jede 3.x = eigener Auftrag mit eigenem Credit-Rahmen)*

## PHASE 4 — Aufräumen / Architektur-Audit (Leichen & Sackgassen)
**Erst NACH DB-Snapshot + Einzel-Freigabe je Punkt.**
| # | Fund | Maßnahme | Risiko |
|---|---|---|---|
| 4.1 | `ARCHITEKTUR_AUDIT.md` erstellen (read-only) | vollständige Bestandsaufnahme | keins (nur Doku) |
| 4.2 | `module_rechnungen` v1 **und** v2 parallel | entscheiden welches System aktiv bleibt, anderes stilllegen | **hoch** (Datenrisiko) |
| 4.3 | `kundenportal` + `portal_v2_backup` | Backup-Modul entfernen wenn ungenutzt | mittel |
| 4.4 | `_archiv` (frontend 28 Dateien + backend) | löschen wenn bestätigt tot | gering–mittel |
| 4.5 | `HelpToggle.jsx` (verwaist seit Glühbirne-Entfernung) | löschen oder bewusst behalten | gering |
| 4.6 | 117 `TODO/FIXME/deprecated`-Marker | sichten, abarbeiten/entfernen | gering |
| 4.7 | Namensraum `customers` vs `module_kunden` | vereinheitlichen | **hoch** (Datenrisiko) |

---

## Empfohlene Reihenfolge (mein Rat)
1. **0.1** Heutiges auf Live bringen (du) → 2. **1.1** Direkter Mailversand (Jimdo) → 3. **4.1** Audit-Doku (gefahrlos) → 4. **1.2** Zahlungseingang → 5. **2.x** Dashboard-Cockpit → 6. **3.x** Automatisierung → 7. **4.2–4.7** Aufräumen (mit Snapshots).

**Begründung:** Erst der tägliche Mailverkehr (1.1, sofort spürbar), dann gefahrlose Bestandsaufnahme (4.1), dann die größte Rechnungswesen-Lücke (1.2). Riskantes Aufräumen (Doppelsysteme/Namensräume) ganz zum Schluss mit Snapshots.

## Nächster konkreter Schritt
**1.1 Direkter Mailversand (Jimdo-SMTP).** Dafür brauche ich von dir die **Jimdo-SMTP-Zugangsdaten**: Server, Port, Benutzer (volle E-Mail), Passwort. *(Die Suite hat bereits funktionierenden SMTP-Versand in `.env` — wir prüfen/aktualisieren nur auf deine Jimdo-Daten und hängen den „Direkt senden"-Button ins UI.)*
